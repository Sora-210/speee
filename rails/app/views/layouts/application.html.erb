<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? "#{yield(:title)} | ベテランすまい" : "ベテランすまい" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "ベテランすまいなら不動産会社を口コミや評判を見て選ぶことができます!" %>">
    <meta name="keywords" content="<%= content_for(:keywords) ? yield(:keywords) : "不動産会社,口コミ,評判" %>">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= yield :no_index %>
    <style>
      .navbar {
        margin-bottom: 20px;
      }
      .header_link {
        text-decoration: none;
        margin-left: 20px;
        color: black;
      }
    </style>    
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <%=link_to "/", class:"header_link" do%>
      <h1>ベテランすまい</h1>
    <%end%>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    </nav>
    <%= yield %>
  </body>
</html>
