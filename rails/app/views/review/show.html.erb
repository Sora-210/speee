<h4 class="fw-bold mt-4 mx-2">ベテランすまい</h3>
  <% branch = @review.branch %>
  <% company = branch.company %>
  <% prefecture_name = @review.prefecture.name %>
  <% city_name = @review.city.name %>

  <h2 class='text-center'><%= @review.title %></h2>
  <div>
  <div class="mx-5 mt-5">
  <p class="lh-sm mx-5 mb-2">売却した不動産会社名: <%= company.name %></p>
  <p class="lh-sm mx-5 mb-2">住所: <%= prefecture_name %><%= city_name %></p>
  <p class="lh-sm mx-5 mb-2">売却の理由:<%= display_sale_reason(@review.sale_reason_type) %></p>
  <% case @review.times_type %>
<% when "first" %>
  <p class="lh-sm mx-5">売却経験:今回の売却が初めて</p>
<% when "second" %>
  <p class="lh-sm mx-5">売却経験:今回の売却が2回目。</p>
<% when "many" %>
  <p class="lh-sm mx-5">売却経験:3回以上</p>
<% end %>
</div>
<div class="mx-5 mt-5">
  <%= render 'review_section', title: '売却時に不安だったこと', content: @review.anxiety %>
  <%= render 'review_section', title: 'この会社に決めた決め手', content: @review.decision_reason %>
  <%= render 'review_section', title: '不動産会社の対応満足度', content: "★#{@review.support_cs} - #{@review.support_reason}" %>
  <%= render 'review_section', title: '今後売却する人へのアドバイス', content: @review.advice %>
  <%= render 'review_section', title: '不動産会社に改善してほしい点', content: @review.request %>
</div>
  <div class="mx-5 mt-5">
  <h5 class="mx-5"><%= company.name %>について</h5>
  <p class="mb-0 mx-5">企業名: <%= company.name %></p>
  <p class="mb-0 mx-5">郵便番号: <%= branch.post_code %></p>
  <p class="mb-0 mx-5">住所: <%= prefecture_name %><%= city_name %><%= branch.address %></p>
  <p class="mb-0 mx-5">電話番号: <%= branch.tel %></p>
  <p class="mb-0 mx-5">FAX: <%= branch.fax %></p>
  <p class="mb-0 mx-5">営業時間: <%= branch.business_hours_text %></p>
  <p class="mb-0 mx-5">定休日: <%= branch.holidays_text %></p>
  </div>
  </div>